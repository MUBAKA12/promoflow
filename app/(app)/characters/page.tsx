export default function CharactersPage() {
  return (
    <div className="p-10">
      <h1 className="font-display text-2xl font-semibold mb-2">Characters</h1>
      <p className="text-[var(--text-muted)]">
        Reusable creator identities (like Luna) with their own before/after asset library — next on the build list.
      </p>
    </div>
  );
}
